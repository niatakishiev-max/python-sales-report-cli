# Python Sales Report CLI

Консольная программа для учёта продаж и формирования простых бизнес-отчётов.

## Возможности

- Добавление продажи
- Просмотр списка продаж
- Подсчёт общей выручки
- Подсчёт общего количества проданных товаров
- Отчёт по товарам с группировкой
- Удаление продажи
- Сохранение продаж в JSON
- Загрузка продаж после перезапуска программы
- Проверка неправильного ввода цены и количества
- Защита от пустого названия товара

## Бизнес-логика

Одна продажа хранится как словарь:

```python
{
    "product": "хлеб",
    "price": 100.0,
    "quantity": 2
}
```

Список продаж хранится в `sales.json`.

Программа умеет считать:

- выручку по каждой продаже;
- общую выручку;
- общее количество проданных товаров;
- отчёт по каждому товару.

## Установка зависимостей

```bash
python -m pip install -r requirements.txt
```

## Запуск

```bash
python sales_report.py
```

## Тесты

Для запуска автотестов:

```bash
python -m pytest
```

Подробнее о тестовых сценариях: [TESTING.md](TESTING.md).

## Используемые технологии

- Python
- pytest
- JSON
- pathlib
- CLI
- Git / GitHub

## Что показывает проект

Проект демонстрирует базовые навыки Python для бизнес-задач:

- списки и словари;
- функции;
- циклы `while` и `for`;
- условия `if / elif / else`;
- обработку ошибок ввода;
- работу с файлами JSON;
- группировку данных по ключу;
- расчёт простых отчётов;
- вынесение бизнес-логики в отдельные функции;
- unit-тесты на `pytest`;
- параметризацию тестов через `pytest.mark.parametrize`.
